import scala.math._

// function
sqrt(4)

// method
BigInt.probablePrime(100, scala.util.Random)

// Methods without parameters can be invoked without (). () only required for mutators
"Hello".distinct

// Methods similar to function calls
"Hello"(0) // Yields 'H'
"Hello".apply(0) // same as above. Any method called apply can be called without .
